apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv-volume2
spec:
  storageClassName: redis-storage
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  nfs:
    path: /opt/kubernetes/volums/redis-data/redis2
    server: 10.25.96.30
